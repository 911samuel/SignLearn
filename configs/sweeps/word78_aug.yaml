sweep_id: word78-aug
trainer: backend/scripts/train_word_model.py
words_file: configs/word78_vocab.txt
epochs: 80
batch_size: 16
seq_len: 80

grid:
  arch:          [tcn]
  learning_rate: [5.0e-4]
  dropout:       [0.4]
  aug_profile:   [baseline, timewarp, mixup_sameclass, "timewarp+mixup"]
